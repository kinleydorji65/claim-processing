

DROP TABLE ACTION_MASTER CASCADE CONSTRAINTS;
DROP TABLE STAGE_MASTER CASCADE CONSTRAINTS;
DROP TABLE CLAIM_TYPE_MASTER CASCADE CONSTRAINTS;
DROP TABLE CLAIM_SOURCE_MASTER CASCADE CONSTRAINTS;
DROP TABLE SUBMISSION_CHANNEL_MASTER CASCADE CONSTRAINTS;
DROP TABLE CESSATION_TYPE_MASTER CASCADE CONSTRAINTS;
DROP TABLE TERMINATION_REASON_TYPE_MASTER CASCADE CONSTRAINTS;
DROP TABLE COMPONENT_GROUP_MASTER CASCADE CONSTRAINTS;
DROP TABLE BENEFIT_TYPE_MASTER CASCADE CONSTRAINTS;
DROP TABLE ACCOUNT_TYPE_MASTER CASCADE CONSTRAINTS;
DROP TABLE RESERVE_ACCOUNT_MASTER CASCADE CONSTRAINTS;
DROP TABLE CLAIM_ELIGIBILITY_MASTER CASCADE CONSTRAINTS;
DROP TABLE CLAIM_ELIGIBILITY_COMPONENT_MAP CASCADE CONSTRAINTS;
DROP TABLE CLAIM_VESTING_RULE_MASTER CASCADE CONSTRAINTS;
DROP TABLE CLAIM_LAPSED_REFUND_MASTER CASCADE CONSTRAINTS;
DROP TABLE COMPONENT_MAPPING_RULE_MASTER CASCADE CONSTRAINTS;
DROP TABLE PARTIAL_WITHDRAWAL_REASON_MASTER CASCADE CONSTRAINTS;
DROP TABLE PARTIAL_WITHDRAWAL_RULE_MASTER CASCADE CONSTRAINTS;
DROP TABLE SPECIAL_CASE_REFUND_AUTHORITY_MASTER CASCADE CONSTRAINTS;
DROP TABLE SPECIAL_CASE_REFUND_REASON_MASTER CASCADE CONSTRAINTS;
DROP TABLE UNCLAIMED_TYPE_MASTER CASCADE CONSTRAINTS;
DROP TABLE UNCLAIMED_STATUS_MASTER CASCADE CONSTRAINTS;
DROP TABLE UNCLAIMED_ACTION_MASTER CASCADE CONSTRAINTS;
DROP TABLE UNCLAIMED_STAGE_MASTER CASCADE CONSTRAINTS;
DROP TABLE UNCLAIMED_NOTICE_TYPE_MASTER CASCADE CONSTRAINTS;
DROP TABLE UNCLAIMED_PERIOD_RULE_MASTER CASCADE CONSTRAINTS;
DROP TABLE UNCLAIMED_INTEREST_FREEZE_RULE_MASTER CASCADE CONSTRAINTS;
DROP TABLE UNCLAIMED_FORFEITURE_RULE_MASTER CASCADE CONSTRAINTS;
DROP TABLE REVIEW_STATUS_MASTER CASCADE CONSTRAINTS;
DROP TABLE DEDUCTION_TYPE_MASTER CASCADE CONSTRAINTS;
DROP TABLE PAYMENT_MODE_MASTER CASCADE CONSTRAINTS;
DROP TABLE PAYMENT_STATUS_MASTER CASCADE CONSTRAINTS;
DROP TABLE LOAN_ADJUSTMENT_PRIORITY_MASTER CASCADE CONSTRAINTS;
DROP TABLE REFUND_TYPE_MASTER CASCADE CONSTRAINTS;
DROP TABLE REFUND_SCOPE_MASTER CASCADE CONSTRAINTS;
DROP TABLE LEGAL_RECOVERY_MASTER CASCADE CONSTRAINTS;

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.ACTION_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(50) NOT NULL UNIQUE,
    NAME                  VARCHAR2(100) NOT NULL,
    DISPLAY_ORDER         NUMBER DEFAULT 1,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.STAGE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(50) NOT NULL UNIQUE,
    NAME                  VARCHAR2(100) NOT NULL,
    DISPLAY_ORDER         NUMBER,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.CLAIM_TYPE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(50) NOT NULL UNIQUE,
    NAME                  VARCHAR2(100) NOT NULL,
    CATEGORY_CODE         VARCHAR2(50),
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.CLAIM_SOURCE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(50) NOT NULL UNIQUE,
    NAME                  VARCHAR2(100) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.SUBMISSION_CHANNEL_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(50) NOT NULL UNIQUE,
    NAME                  VARCHAR2(100) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.CESSATION_TYPE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(50) NOT NULL UNIQUE,
    NAME                  VARCHAR2(100) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.TERMINATION_REASON_TYPE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(60) NOT NULL UNIQUE,
    NAME                  VARCHAR2(120) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.COMPONENT_GROUP_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(50) NOT NULL UNIQUE,
    NAME                  VARCHAR2(100) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.BENEFIT_TYPE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(50) NOT NULL UNIQUE,
    NAME                  VARCHAR2(100) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.ACCOUNT_TYPE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(40) NOT NULL UNIQUE,
    NAME                  VARCHAR2(100) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.RESERVE_ACCOUNT_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    RESERVE_ACCOUNT_CODE  VARCHAR2(50) NOT NULL UNIQUE,
    RESERVE_ACCOUNT_NAME  VARCHAR2(150) NOT NULL,
    ACCOUNT_TYPE_CODE     VARCHAR2(40) NOT NULL,
    SCHEME_TYPE_CODE      VARCHAR2(30),
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.CLAIM_ELIGIBILITY_MASTER (
    ID                        NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    RULE_CODE                 VARCHAR2(50) NOT NULL UNIQUE,
    RULE_NAME                 VARCHAR2(200) NOT NULL,
    CLAIM_CATEGORY_CODE       VARCHAR2(50),
    MEMBER_TYPE               VARCHAR2(50),
    CESSATION_TYPE            VARCHAR2(50),
    SCHEME_TYPE               VARCHAR2(30),
    MIN_CONTRIBUTION_MONTHS   NUMBER,
    MAX_CONTRIBUTION_MONTHS   NUMBER,
    EFFECTIVE_FROM            DATE,
    EFFECTIVE_TO              DATE,
    IS_ACTIVE                 CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT                TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY                VARCHAR2(100),
    UPDATED_AT                TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY                VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.CLAIM_ELIGIBILITY_COMPONENT_MAP (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    RULE_ID               NUMBER NOT NULL,
    BENEFIT_TYPE          VARCHAR2(50),
    COMPONENT_CODE        VARCHAR2(30),
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100),
    CONSTRAINT FK_CLAIM_ELIGIBILITY_MAP_RULE
        FOREIGN KEY (RULE_ID) REFERENCES CLAIM_ELIGIBILITY_MASTER(ID)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.CLAIM_VESTING_RULE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    RULE_CODE             VARCHAR2(50) NOT NULL UNIQUE,
    MEMBER_CATEGORY       VARCHAR2(50),
    EFFECTIVE_FROM        DATE,
    EFFECTIVE_TO          DATE,
    REFUND_TYPE           VARCHAR2(50),
    MIN_VESTING_MONTHS    NUMBER,
    MAX_VESTING_MONTHS    NUMBER,
    COMPARISON_TYPE       VARCHAR2(30),
    PAYOUT_RESULT         VARCHAR2(50),
    REMARKS               VARCHAR2(500),
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.CLAIM_LAPSED_REFUND_MASTER (
    ID                        NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CLAIM_CATEGORY_CODE       VARCHAR2(50),
    MEMBER_TYPE               VARCHAR2(50),
    CESSATION_TYPE            VARCHAR2(50),
    SCHEME_TYPE               VARCHAR2(30),
    MIN_CONTRIBUTION_MONTHS   NUMBER,
    MAX_CONTRIBUTION_MONTHS   NUMBER,
    EFFECTIVE_FROM            DATE,
    EFFECTIVE_TO              DATE,
    LAPSED_FORMULA            VARCHAR2(500),
    IS_ACTIVE                 CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT                TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY                VARCHAR2(100),
    UPDATED_AT                TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY                VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.COMPONENT_MAPPING_RULE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    RULE_TYPE             VARCHAR2(50) NOT NULL,
    RULE_ID               NUMBER NOT NULL,
    COMPONENT_CODE        VARCHAR2(30) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.PARTIAL_WITHDRAWAL_REASON_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(50) NOT NULL UNIQUE,
    NAME                  VARCHAR2(150) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.PARTIAL_WITHDRAWAL_RULE_MASTER (
    ID                                NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    RULE_CODE                         VARCHAR2(50) NOT NULL UNIQUE,
    RULE_NAME                         VARCHAR2(200) NOT NULL,
    WITHDRAWAL_CATEGORY               VARCHAR2(50),
    MAX_WITHDRAWAL_PERCENTAGE         NUMBER(5,2),
    MIN_PF_ACCUMULATION               NUMBER(18,2),
    MIN_TOTAL_ACCUMULATION            NUMBER(18,2),
    MIN_CONTRIBUTION_MONTHS           NUMBER,
    UNEMPLOYMENT_MIN_MONTHS           NUMBER,
    ACTIVE_EMPLOYMENT_ALLOWED         CHAR(1) DEFAULT 'N' CHECK (ACTIVE_EMPLOYMENT_ALLOWED IN ('Y','N')),
    DUPLICATE_ACTIVE_REQUEST_ALLOWED  CHAR(1) DEFAULT 'N' CHECK (DUPLICATE_ACTIVE_REQUEST_ALLOWED IN ('Y','N')),
    EFFECTIVE_FROM                    DATE,
    EFFECTIVE_TO                      DATE,
    IS_ACTIVE                         CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT                        TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY                        VARCHAR2(100),
    UPDATED_AT                        TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY                        VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.SPECIAL_CASE_REFUND_AUTHORITY_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(50) NOT NULL UNIQUE,
    NAME                  VARCHAR2(150) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.SPECIAL_CASE_REFUND_REASON_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(60) NOT NULL UNIQUE,
    NAME                  VARCHAR2(150) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

-- UNCLAIMED MASTER
CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.UNCLAIMED_TYPE_MASTER (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE VARCHAR2(60) NOT NULL UNIQUE,
    NAME VARCHAR2(150) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.UNCLAIMED_STATUS_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(60) NOT NULL UNIQUE,
    NAME                  VARCHAR2(150) NOT NULL,
    DISPLAY_ORDER         NUMBER,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.UNCLAIMED_ACTION_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(60) NOT NULL UNIQUE,
    NAME                  VARCHAR2(150) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.UNCLAIMED_STAGE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(60) NOT NULL UNIQUE,
    NAME                  VARCHAR2(150) NOT NULL,
    DISPLAY_ORDER         NUMBER,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.UNCLAIMED_NOTICE_TYPE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(40) NOT NULL UNIQUE,
    NAME                  VARCHAR2(100) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.UNCLAIMED_PERIOD_RULE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    RULE_NAME             VARCHAR2(100) NOT NULL UNIQUE,
    PERIOD_VALUE          NUMBER NOT NULL,
    PERIOD_UNIT           VARCHAR2(20) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.UNCLAIMED_INTEREST_FREEZE_RULE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(60) NOT NULL UNIQUE,
    NAME                  VARCHAR2(150) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.UNCLAIMED_FORFEITURE_RULE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(60) NOT NULL UNIQUE,
    NAME                  VARCHAR2(150) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.REVIEW_STATUS_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(60) NOT NULL UNIQUE,
    NAME                  VARCHAR2(150) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

-- Deduction Type Master
CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.DEDUCTION_TYPE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(50) NOT NULL UNIQUE,
    NAME                  VARCHAR2(150) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.PAYMENT_MODE_MASTER (
    ID                   NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                 VARCHAR2(40) NOT NULL UNIQUE,
    NAME                 VARCHAR2(100) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.PAYMENT_STATUS_MASTER (
    ID                   NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                 VARCHAR2(40) NOT NULL UNIQUE,
    NAME                 VARCHAR2(100) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

-- LOAN PRIORITY
CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.LOAN_ADJUSTMENT_PRIORITY_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(60) NOT NULL UNIQUE,
    NAME                  VARCHAR2(150) NOT NULL,
    IS_APPLICABLE         CHAR(1) DEFAULT 'Y' CHECK (IS_APPLICABLE IN ('Y','N')),
    PRIORITY_SEQUENCE     NUMBER NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

-- Refund Type Master
CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.REFUND_TYPE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(60) NOT NULL UNIQUE,
    NAME                  VARCHAR2(150) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.REFUND_SCOPE_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(40) NOT NULL UNIQUE,
    NAME                  VARCHAR2(100) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);

-- LEGAL MASTER 
CREATE TABLE PPFMS_CLAIMS_WORKFLOW_SERVICE_SCHEMA.LEGAL_RECOVERY_MASTER (
    ID                    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE                  VARCHAR2(40) NOT NULL UNIQUE,
    NAME                  VARCHAR2(100) NOT NULL,
    IS_ACTIVE             CHAR(1) DEFAULT 'Y' CHECK (IS_ACTIVE IN ('Y','N')),
    CREATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    CREATED_BY            VARCHAR2(100),
    UPDATED_AT            TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_BY            VARCHAR2(100)
);